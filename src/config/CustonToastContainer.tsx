import { ToastContainer } from 'react-toastify';

/**
 * Componet de configuração da biblioteca de Toas (notificações)
 */
const CustonToastContainer = () => {
  return <ToastContainer position="top-center" />;
};

export default CustonToastContainer;
