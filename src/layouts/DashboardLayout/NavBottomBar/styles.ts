import styled from 'styled-components';
import { BottomNavigation as BottomNavigationMUI } from '@material-ui/core';

export const BottomNavigation = styled(BottomNavigationMUI)`
  width: 100%;
  position: absolute;
  bottom: 0;
`;
